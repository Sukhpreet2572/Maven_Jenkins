pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build Successful'
            }
        }

    }
}
// docker stop wordpress-project-wordpress-1
// docker stop wordpress-project-db-1
// docker rm -f jenkins
// docker run -d --name jenkins -p 8080:8080 -p 50000:50000 -v jenkins_home:/var/jenkins_home jenkins/jenkins:lts